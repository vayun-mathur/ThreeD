vec3 value;
value.x = 0.2;
value.y = 0.2;
this.parent.speed.value = value;

this.parent.click = {
	if(button)
		this.mesh2.position += this.speed.value;
	else if(true)
		this.mesh2.position -= this.speed.value;
};